name: Metadata Parser Validation for Preprocessor output

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Parse preprocessor test output
      run: dotnet run ./tools/MetadataParser/MetadataParser.csproj $GITHUB_WORKSPACE/transforms/csdl/preprocess_csdl_test_output.xml
